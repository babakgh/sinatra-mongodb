default: &default
  sinatra:
    configuration:
      custom_deploy_command: env|grep -E "^RY_|^ALGOLIA_|^IMG_" > .env.local

feature-testing:
  <<: *default
  sinatra:
    configuration:
      custom_deploy_command: env|grep -E "^RY_|^ALGOLIA_|^IMG_" > .env.local; redis-cli flushall

staging:
  <<: *default
  sinatra:
    configuration:
      custom_deploy_command: env|grep -E "^RY_|^ALGOLIA_|^IMG_" > .env.local; redis-cli flushall

production:
  <<: *default
